package com.example.pizzeria.config;

/**
 * Legacy placeholder: OpenAPI setup now lives in {@link OpenApiConfig}.
 *
 * <p>The class remains so that any historical imports continue to compile, but
 * it deliberately provides no Spring beans.</p>
 */
public class SwaggerConfig {
        private SwaggerConfig() {
                throw new UnsupportedOperationException("Use OpenApiConfig instead");
        }
}
